<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security 
		http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- 비밀번호 암호화 Bean (BCrypt) -->
	<bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
	
	<!-- 정적 리소스 보안 제외 -->
	<security:http pattern="/resources/**" security="none"/>
	
	<security:http auto-config="true" use-expressions="true">
		<!-- 인증이 필요 없는 페이지 (회원가입, 로그인 관련) -->
		<security:intercept-url pattern="/" access="permitAll"/>
		<security:intercept-url pattern="/join" access="permitAll"/>
		<security:intercept-url pattern="/joinPro" access="permitAll"/>
		<security:intercept-url pattern="/login" access="permitAll"/>
		<security:intercept-url pattern="/checkEmail" access="permitAll"/>
		
		<!-- 나머지 모든 요청은 인증 필요 -->
		<security:intercept-url pattern="/**" access="hasRole('USER')"/>
		
		<!-- 로그인 설정 -->
		<security:form-login login-page="/login" login-processing-url="/loginPro"
			username-parameter="email" password-parameter="password"
			default-target-url="/main" authentication-failure-url="/login?error=true" always-use-default-target="false"/>
		
		<!-- 로그아웃 설정 -->
		<security:logout logout-url="/logout" logout-success-url="/" invalidate-session="true" delete-cookies="JSESSIONID"/>
			
		<!-- CSRF 보호 활성화 -->
		<security:csrf/>
		
		<!-- 접근 거부 처리 -->
		<security:access-denied-handler error-page="/accessDenied"/>
		
		<!-- 세션 관리 -->
		<security:session-management>
			<security:concurrency-control max-sessions="1" expired-url="/login?expired=true"/>
		</security:session-management>
	</security:http>
	
	<!-- 인증 관리자 설정 -->
	<security:authentication-manager>
		<security:authentication-provider user-service-ref="customUserDetailsService">
			<security:password-encoder ref="passwordEncoder"/>
		</security:authentication-provider>
	</security:authentication-manager>

	<!-- Custom UserDetailsService Bean 등록 -->
	<bean id="customUserDetailsService" class="com.itwillbs.security.CustomUserDetailsService"/>

</beans>